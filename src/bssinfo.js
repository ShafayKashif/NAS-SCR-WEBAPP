import React from 'react';
import './summarypg.css';
import Homepage from './homepage';

const BSSInfo = () => {
    return (
        <div className="summary-page">
            <Homepage />
            <h1 className="heading">BSS Officer Information</h1>
            </div>
    );
};

export default BSSInfo;